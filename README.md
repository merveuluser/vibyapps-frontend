# Viby Apps

A small React landing site for vibe-coded mobile apps.

## Development

```bash
npm install
npm run dev
```

## Production Build

```bash
npm run build
```

The project includes a GitHub Actions workflow for GitHub Pages deployment.
Because `public/CNAME` is set to `vibyapps.com`, configure your domain DNS to point at GitHub Pages when you are ready to publish.
